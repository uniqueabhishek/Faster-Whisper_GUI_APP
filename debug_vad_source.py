import faster_whisper.vad
import inspect

try:
    print("Source code for faster_whisper.vad.SileroVADModel.__call__:")
    print(inspect.getsource(faster_whisper.vad.SileroVADModel.__call__))
except Exception as e:
    print(f"Could not get source: {e}")

try:
    print("\nSource code for faster_whisper.vad.get_speech_timestamps:")
    print(inspect.getsource(faster_whisper.vad.get_speech_timestamps))
except Exception as e:
    print(f"Could not get source: {e}")
